services:
  backend:
    restart: always
    build:
      context: .
      target: development
    volumes:
      - ./src:/usr/src/app/src
    networks:
      - bus
    deploy:
      mode: replicated
      replicas: 2
networks:
  bus:
